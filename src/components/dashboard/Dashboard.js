import React, { Component } from "react";

// Including all the other components that will be displayed here

class Dashboard extends Component {
  render() {
    return (
      <div>
        <p>This is the dashbaord. Its not 100% yet, so give it some time! :)</p>
      </div>
    );
  }
}

export default Dashboard;
